[project]
name = "6p5-quest-scripts"
version = "0.1.0"
description = "Audio and utility scripts"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pydub>=0.25.1",
]

[tool.uv]
dev-dependencies = []
